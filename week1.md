# Git Workbook Week 1 정리

## Git이란 무엇인가?
* **Git**은 파일의 변경 사항을 추적하고 여러 사용자가 파일을 함께 작업할 수 있도록 돕는 **분산 버전 관리 시스템**이다.
 - 버전 관리: 작업 내용의 수정, 추가, 삭제를 기록하여 특정 ㅣ점의 상태로 되돌아갈 수 있다. 파일을 여러 개 만들 필요 없이 한 파일 내의 모든 변경 이력을 체계적으로 관리할 수 있어 불필요한 저장 공간의 낭비를 막아준다.
 - 협업: GitHub와 같은 **원격 저장소**를 활용해 여러 사용자가 같은 파일을 동시에 작업하고, 작업 내용을 동기화할 수 있다. 각자의 로컬(컴퓨터)에서 작업한 내용을 원격 저장소에 공유함으로써 모든 팀원이 동일한 최신 작업 내용을 공유하며 효율적으로 협업할 수 있다.
 - 분산 관리: 중앙 서버에 의존하지 않고 각 사용자의 컴퓨터에 모든 버정 이력을 복제하여 관리하므로, 인터넷 연결이 끊어져도 작업이 가능하며, 중앙 서버에 문제가 발생해도 다른 사용자의 컴퓨터에 있는 버전 이력을 통해 복구가 가능하다.


## Git을 통한 파일의 관리 방식
1. **Working Directory (워킹 디렉토리)**: 내가 현재 작업하고 있는 공간(폴더). 기본적으로 파일들의 변경 사항이 모두 추적되지만, .gitignore로 처리된 파일은 추적되지 않고, 원격 레포에도 올라가지 않는다.
2. **Staging Area (스테이징 영역)**: 커밋을 위한 준비 공간. 작업한 모든 파일을 커밋하는 대신, 커밋하고 싶은 파일만 선택적으로 준비(stage)할 수 있다. git add 명령어가 이 역할을 수행한다.
3. **Local Repository (로컬 저장소)**: 커밋을 통해 파일의 변경 사항을 기록하는 곳. git commit 명령어가 이 역할을 수행한다. 
4. **Remote Repository (원격 저장소)**: GitHub와 같은 인터넷 상의 원격 저장소. 로컬 저장소의 내용을 푸시하여 업로드하고 팀원들과 공유할 수 있다. git push 명령어가 이 역할을 수행한다. 또한 반대로, 원격 저장소의 최신 내용을 로컬로 풀해서 가져올 때는 git pull 명령어를 사용한다.


## Git 사용법
1. 원격 레포 생성 및 로컬과 연결: GitHub에서 New 버튼을 눌러 새로운 레포지토리를 만든다. `git clone (레포지토리[원격 저장소] 주소)`로 원격 저장소를 내 컴퓨터에 복제하여 로컬 저장소를 만든다.
2. `.gitignore`로 파일 추적되지 않게 관리: 디렉토리 내에서 추적되지 않아야 하는 파일 목록을 지정하는 데 쓰인다. 폴더에서 터미널을 열고 `touch .gitignore`을 작성하면 폴더에서 `.gitignore` 파일이 생긴다. 특정 파일을 추적해제하고 싶다면 해당 파일명을 적고 저장하거나, 특정 폴더나 확장자를 추적해제하고 싶다면 `*.(확장자 이름)`, `(폴더 이름)/` 형식으로 작성하고 저장하면 된다.
3. `add, commit, push`로 추적 상태 관리
 * `add`
  - `git add .`: 모든 파일을 한 번에 add 해서 staged 상태로 변경
  - `git add (파일 이름)`: 특정 파일만 add 해서 staged 상태로 변경
 * `commit`: staged된 파일들의 변경 사항을 로컬 레포에 반영
  - `git commit -m"(커밋 메시지)"`
 * `push`: 로컬 레포에 반영된 내용을 원격 레포에 전송
  - `git push origin (브랜치 이름)`
4. 내 파일 상태 확인하기: `git status`


## 브랜치 (Branch)
* 브랜치는 하나의 프로젝트에서 여러 개발자가 동시에 독립적인 작업을 할 수 있도록 분리된 작업 공간이다. 나뭇가지가 여러 방향으로 뻗어나가듯이, 메인 작업 흐름 (= main branch) 에서 분리되어 각자 개발하고, 작업이 완료되면 다시 main branch와 합친다.
 - 브랜치 생성: `git branch (브랜치 이름)`
 - 브랜치 전환: `git switch (브랜치 이름)` (checkout을 써도 되지만, switch를 더 추천!)
 - 브랜치 병합: `git merge (병합할 브랜치 이름)`


## 커밋 기록 확인하기
1. 로그 조회: 커밋들의 기록을 살펴본다. `git log`을 터미널에 입력하면 볼 수 있다. 커밋 해시(커밋을 구분하기 위한 고유한 ID), 커밋 생성자, 커밋 일시, 커밋 메시지로 구성된다.
2. `HEAD`: 내 브랜치의 최신 커밋을 참고하는 값. 즉, 내가 위치하고 있는 브랜치의 최신 커밋을 포인터로 가리키는 것


## Issue, PR
1. Issue (이슈): 프로젝트 내에서 기능 구현, 버그 수정, 개선 사항 등 해야 할 일을 기록하고 관리하는 데 사용된다. 팀원 간에 작업 내용을 공유하고 담당자를 할당하는 등의 용도로 활용된다.
2. PR = Pull Request (풀 리퀘스트): 자신이 작업한 브랜치의 내용을 main branch에 병합해 달라고 요청하는 기능이다. 이 과정을 통해 다른 팀원들이 코드를 검토하고 피드백을 주고받을 수 있어 협업을 원활하게 만든다. PR이 승인되면 변경 내용이 main branch에 반영된다. 